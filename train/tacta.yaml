# Tacta Board Game Scoring Dataset Configuration

# Path to the dataset root (optional if absolute paths are used in train/val)
path: /Users/mpb/WorkSpace/local_job/synthesis_data/dataset

# Train and Validation splits (using text files with absolute paths)
# Train and Validation splits (using text files with absolute paths)
train: /Users/mpb/WorkSpace/local_job/train/train_full.txt
val: /Users/mpb/WorkSpace/local_job/train/val_full.txt

# Class names
names:
  0: Score_1
  1: Score_2
  2: Score_3
  3: Score_4
